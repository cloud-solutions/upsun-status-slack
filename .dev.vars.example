# Copy to `.dev.vars` for local `wrangler dev` runs (gitignored).
# In production, set this with: wrangler secret put SLACK_WEBHOOK_URL
SLACK_WEBHOOK_URL="https://hooks.slack.com/services/XXX/YYY/ZZZ"
