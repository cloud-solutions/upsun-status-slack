{
  "name": "upsun-slack",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that polls Upsun's status page and posts to Slack",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "reset-state": "node scripts/reset-state.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250414.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
